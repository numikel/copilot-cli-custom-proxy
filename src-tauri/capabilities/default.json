{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Uprawnienia okna ustawień: wywoływanie komend aplikacji.",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
