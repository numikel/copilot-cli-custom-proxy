{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Settings window permissions: invoking app commands.",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
